
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	
	СписокСобытий = Параметры.СписокСобытий.Скопировать();
	
	ПредставлениеДаты = Формат(НачалоДня(Параметры.Дата),"ДФ='dddd, d MMMM yyyy'");
	
	Заголовок = ПредставлениеДаты + " список событий на весь день";
	
	
КонецПроцедуры
