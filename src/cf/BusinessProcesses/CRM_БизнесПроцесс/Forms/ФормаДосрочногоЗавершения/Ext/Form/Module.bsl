
&НаКлиенте
Процедура ОК(Команда)
	
	СтруктураВозврата = Новый Структура();
	СтруктураВозврата.Вставить("ЗавершенДосрочно"				,Истина);
	СтруктураВозврата.Вставить("ВариантЗавершения"				,ПредопределенноеЗначение("Справочник.CRM_ВариантыЗавершенияБизнесПроцесса.Неудачно"));
	СтруктураВозврата.Вставить("ПричинаДосрочногоЗавершения"	,ПричинаНеудачногоЗавершения);
	
	Закрыть(СтруктураВозврата);
	
КонецПроцедуры

&НаКлиенте
Процедура Отмена(Команда)
	Закрыть();
КонецПроцедуры
