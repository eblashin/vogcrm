
&НаКлиенте
Процедура Ок(Команда)
	
	Закрыть(Новый Структура("Описание,ТипПеремещения",Описание,ТипПеремещения));
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("Описание") тогда
		Описание = Параметры.Описание;
		ТипПеремещения = Параметры.ТипПеремещения;
		Сотрудник = Параметры.Сотрудник;
	Иначе
		Сотрудник = Пользователи.ТекущийПользователь();
	КонецЕсли;
	
КонецПроцедуры
