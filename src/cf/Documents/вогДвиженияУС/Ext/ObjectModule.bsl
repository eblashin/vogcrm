
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если ОбменДанными.Загрузка Тогда 
		Возврат
	КонецЕсли;
	
	УстановитьАктивностьУНаборовДвижений(Не ПометкаУдаления);
		
КонецПроцедуры

//Установим активность движений в соответствии с пометкой удаления документа
Процедура УстановитьАктивностьУНаборовДвижений(Активность)
	
	Для Каждого Набор Из ЭтотОбъект.Движения Цикл
		Если НЕ Набор.Модифицированность() Тогда 
			Набор.Прочитать();
			Набор.Записывать = Истина;
		КонецЕсли;
		Набор.УстановитьАктивность(Активность);
		
	КонецЦикла;
	
КонецПроцедуры

	
