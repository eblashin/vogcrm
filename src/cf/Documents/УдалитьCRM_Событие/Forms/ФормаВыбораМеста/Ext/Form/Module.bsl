
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("СписокАдресов") Тогда
		СписокАдресов = Параметры.СписокАдресов;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура СписокАдресовВыборЗначения(Элемент, Значение, СтандартнаяОбработка)
	
	Если НЕ Элементы.СписокАдресов.ТекущиеДанные = Неопределено Тогда
		ЭтаФорма.Закрыть(Элементы.СписокАдресов.ТекущиеДанные.Значение.Представление);
	Иначе
		ЭтаФорма.Закрыть();
	КонецЕсли;
	
КонецПроцедуры
