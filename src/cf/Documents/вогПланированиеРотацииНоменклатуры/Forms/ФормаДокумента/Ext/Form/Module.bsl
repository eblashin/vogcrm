
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	//Вставить содержимое обработчика
	СпособУправленияНоменклатурой = Объект.Направление.СпособУправленияНоменклатурой;
	Если СпособУправленияНоменклатурой = Перечисления.вогСпособыУправленияНоменклатурой.Номенклатура Тогда
		Элементы.Номенклатура.Видимость = Истина;
		
	Иначе
		Элементы.Панель.Видимость = Истина;
	КонецЕсли;	 
	
	
КонецПроцедуры
