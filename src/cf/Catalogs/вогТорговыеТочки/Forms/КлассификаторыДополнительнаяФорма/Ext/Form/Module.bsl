
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ОбъектСсылка = Неопределено;
	Параметры.Свойство("ОбъектСсылка",ОбъектСсылка);
	Если НЕ ЗначениеЗаполнено(ОбъектСсылка) Тогда
		Отказ = Истина;
		Возврат;
	КонецЕсли; 
	
	//+Классификаторы
	//CRM_КлассификаторыВызовСервера.ФормаОбъектаПриЧтенииНаСервере(ЭтаФорма, ОбъектСсылка);
	//-Классификаторы
	
КонецПроцедуры
