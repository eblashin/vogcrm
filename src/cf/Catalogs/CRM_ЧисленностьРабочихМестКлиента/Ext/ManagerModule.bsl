
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

// Процедура создает элементы справочника по умолчанию.
//
// Параметры:
//	Нет.
//
Процедура СоздатьЭлементыСправочникаПоУмолчанию() Экспорт
	
	// Элемент "<= 10"
	Элемент	= Справочники.CRM_ЧисленностьРабочихМестКлиента.НайтиПоНаименованию("<= 10", Истина);
	Если НЕ ЗначениеЗаполнено(Элемент) Тогда
		НовыйЭлемент				= Справочники.CRM_ЧисленностьРабочихМестКлиента.СоздатьЭлемент();
		НовыйЭлемент.Наименование	= НСтр("ru='<= 10'");
		НовыйЭлемент.Записать();
	КонецЕсли; 
	// Элемент ">10"
	Элемент	= Справочники.CRM_ЧисленностьРабочихМестКлиента.НайтиПоНаименованию(">10", Истина);
	Если НЕ ЗначениеЗаполнено(Элемент) Тогда
		НовыйЭлемент				= Справочники.CRM_ЧисленностьРабочихМестКлиента.СоздатьЭлемент();
		НовыйЭлемент.Наименование	= НСтр("ru='>10'");
		НовыйЭлемент.Записать();
	КонецЕсли; 
	// Элемент ">50"
	Элемент	= Справочники.CRM_ЧисленностьРабочихМестКлиента.НайтиПоНаименованию(">50", Истина);
	Если НЕ ЗначениеЗаполнено(Элемент) Тогда
		НовыйЭлемент				= Справочники.CRM_ЧисленностьРабочихМестКлиента.СоздатьЭлемент();
		НовыйЭлемент.Наименование	= НСтр("ru='>50'");
		НовыйЭлемент.Записать();
	КонецЕсли; 
	// Элемент ">100"
	Элемент	= Справочники.CRM_ЧисленностьРабочихМестКлиента.НайтиПоНаименованию(">100", Истина);
	Если НЕ ЗначениеЗаполнено(Элемент) Тогда
		НовыйЭлемент				= Справочники.CRM_ЧисленностьРабочихМестКлиента.СоздатьЭлемент();
		НовыйЭлемент.Наименование	= НСтр("ru='>100'");
		НовыйЭлемент.Записать();
	КонецЕсли; 
	// Элемент ">500"
	Элемент	= Справочники.CRM_ЧисленностьРабочихМестКлиента.НайтиПоНаименованию(">500", Истина);
	Если НЕ ЗначениеЗаполнено(Элемент) Тогда
		НовыйЭлемент				= Справочники.CRM_ЧисленностьРабочихМестКлиента.СоздатьЭлемент();
		НовыйЭлемент.Наименование	= НСтр("ru='>500'");
		НовыйЭлемент.Записать();
	КонецЕсли; 
	
КонецПроцедуры // СоздатьЭлементыСправочникаПоУмолчанию()

#КонецЕсли


// Процедура - обработчик получения формы справочника.
//
// Параметры:
//	ВидФормы					- Строка	- Вид формы справочника.
//	Параметры					- Структура	- Параметры, переданные для формы.
//	ВыбраннаяФорма				- Строка	- Имя открываемой формы справочника.
//	ДополнительнаяИнформация	- Строка	- Дополнительная информация.
//	СтандартнаяОбработка		- Булево	- Признак стандартной обработки.
//
Процедура ОбработкаПолученияФормы(ВидФормы, Параметры, ВыбраннаяФорма, ДополнительнаяИнформация, СтандартнаяОбработка)
	Если ВидФормы = "ФормаОбъекта" Тогда
		Если Параметры.Свойство("Ключ") Тогда
			Если Параметры.Ключ.Предопределенный Тогда
				// Запрещаем редактировать предопределенный элемент.
				СтандартнаяОбработка = Ложь;
				ВыбраннаяФорма = "ФормаЭлемента";
			КонецЕсли;
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры // ОбработкаПолученияФормы()
