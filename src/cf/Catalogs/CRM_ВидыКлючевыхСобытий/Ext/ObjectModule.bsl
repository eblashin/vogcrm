
Процедура ПередЗаписью(Отказ)
	
	// порядок
	Если Ссылка.Пустая() Тогда
		Запрос = Новый Запрос;
		Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 1
		|	CRM_ВидыКлючевыхСобытий.РеквизитДопУпорядочивания КАК РеквизитДопУпорядочивания
		|ИЗ
		|	Справочник.CRM_ВидыКлючевыхСобытий КАК CRM_ВидыКлючевыхСобытий
		|
		|УПОРЯДОЧИТЬ ПО
		|	РеквизитДопУпорядочивания УБЫВ";
		
		РезультатЗапроса = Запрос.Выполнить();
		
		Если НЕ РезультатЗапроса.Пустой() Тогда
			РеквизитДопУпорядочивания = РезультатЗапроса.Выгрузить()[0].РеквизитДопУпорядочивания + 1;
		Иначе
			РеквизитДопУпорядочивания = 1;
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры
