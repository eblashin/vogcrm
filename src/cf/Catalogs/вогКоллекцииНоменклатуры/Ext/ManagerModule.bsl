#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)

КонецПроцедуры

#КонецОбласти

#Область ЗаполнениеДанныхОбъектов

Процедура ЗаполнитьУровеньГруппировкиКоллекции(ОбъектКоллекция) Экспорт

	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("Направление", ОбъектКоллекция.НаправлениеДеятельности);
	Запрос.УстановитьПараметр("Уровень"	   , ОбъектКоллекция.Уровень());
	
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	ПравилаФормированияУровнейГруппировок.УровеньГруппировки КАК УровеньГруппировки
		|ИЗ
		|	Справочник.НаправленияДеятельности.ПравилаФормированияУровнейГруппировок КАК ПравилаФормированияУровнейГруппировок
		|ГДЕ
		|	ПравилаФормированияУровнейГруппировок.Ссылка = &Направление
		|	И ПравилаФормированияУровнейГруппировок.НомерСтроки - 1 = &Уровень";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	Если Выборка.Следующий() Тогда
		ОбъектКоллекция.Уровень = Выборка.УровеньГруппировки;
		
	КонецЕсли;

КонецПроцедуры

#КонецОбласти

#КонецЕсли