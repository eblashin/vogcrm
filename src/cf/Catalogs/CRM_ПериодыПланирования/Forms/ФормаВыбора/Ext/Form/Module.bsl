
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ИсключатьФактическийПериод") И Параметры.ИсключатьФактическийПериод Тогда
		Список.Параметры.УстановитьЗначениепараметра("ИсключатьФактическийПериод", Истина);
	Иначе
		Список.Параметры.УстановитьЗначениепараметра("ИсключатьФактическийПериод", Ложь);
	КонецЕсли;
		
КонецПроцедуры
