#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда



Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Если ЗначениеЗаполнено(ДанныеЗаполнения) И ТипЗнч(ДанныеЗаполнения) = Тип("Структура") Тогда
		Если ТипЗнч(ДанныеЗаполнения.Объект) = Тип("Строка") Тогда
			Объект = ПолучитьИзВременногоХранилища(ДанныеЗаполнения.Объект).ОбъектЗначение;
		Иначе
			Объект = ДанныеЗаполнения.Объект;
		КонецЕсли;
		ИмяОбъекта = Объект.Метаданные().Имя;
	КонецЕсли;
	
КонецПроцедуры

#КонецЕсли