
&НаСервере
// Процедура - обработчик события формы "ПриСозданииНаСервере".
//
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//ЭтаФорма.РазрешеноРедактированиеБалла		= ПравоДоступа("Редактирование",Метаданные.Константы.уэкДлительностьБаллаАвтомотиватора);
	//ЭтаФорма.уэкДлительностьБаллаАвтомотиватора	= Константы.уэкДлительностьБаллаАвтомотиватора.Получить();
	
	//Элементы.уэкДлительностьБаллаАвтомотиватора.ТолькоПросмотр	= Не ЭтаФорма.РазрешеноРедактированиеБалла;
	
	Если Параметры.Свойство("Подразделение") Тогда 
		ЭлементОтбора = Список.Отбор.Элементы.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
		ЭлементОтбора.ЛевоеЗначение = Новый ПолеКомпоновкиДанных("Подразделение");
		ЭлементОтбора.ВидСравнения = ВидСравненияКомпоновкиДанных.Равно;
		ЭлементОтбора.Использование = Истина;
		ЭлементОтбора.ПравоеЗначение = Параметры.Подразделение;	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
// Процедура - обработчик события формы "ПриЗакрытии".
//
Процедура ПриЗакрытии(ЗавершениеРаботы)
	Если ЗавершениеРаботы Тогда Возврат; КонецЕсли;
	//ПриЗакрытииНаСервере();
КонецПроцедуры

&НаСервере
// Серверный обработчик события формы "ПриЗакрытии".
//
Процедура ПриЗакрытииНаСервере()
	
	//Если ЭтаФорма.РазрешеноРедактированиеБалла Тогда
	//
	//	Константы.уэкДлительностьБаллаАвтомотиватора.Установить(ЭтаФорма.уэкДлительностьБаллаАвтомотиватора);
	//
	//КонецЕсли; 
	
КонецПроцедуры

