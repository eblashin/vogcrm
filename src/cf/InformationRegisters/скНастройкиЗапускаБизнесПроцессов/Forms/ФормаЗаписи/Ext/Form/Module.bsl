
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	Элементы.КартаМаршрута.Видимость = Не Параметры.ЗначенияЗаполнения.Свойство("КартаМаршрута") И ЗначениеЗаполнено(Запись.Настройка);
	Элементы.Настройка.Видимость	 = Не Параметры.ЗначенияЗаполнения.Свойство("Настройка") И ЗначениеЗаполнено(Запись.КартаМаршрута);
	
КонецПроцедуры
	
#КонецОбласти
