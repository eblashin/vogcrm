Процедура ДобавитьРасчетПоКонтактнымЛицам(Ссылка,ИндВсего, Инд) Экспорт
	
	Если ЗначениеЗаполнено(Ссылка) Тогда
		
		ВыборкаКЛ = Справочники.КонтактныеЛицаПартнеров.Выбрать(, Ссылка);
		
		Пока ВыборкаКЛ.Следующий() Цикл
			ИндВсего = ИндВсего + 4;
			Если ЗначениеЗаполнено(ВыборкаКЛ.CRM_ТипОтношенийПредставление) Тогда
				Инд = Инд + 1;
			КонецЕсли;
			Если ЗначениеЗаполнено(ВыборкаКЛ.CRM_Должность) Тогда
				Инд = Инд + 1;
			КонецЕсли;
			Если ЗначениеЗаполнено(ВыборкаКЛ.Пол) Тогда
				Инд = Инд + 1;
			КонецЕсли;
			Если ЗначениеЗаполнено(ВыборкаКЛ.CRM_Состояние) Тогда
				Инд = Инд + 1;
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ДобавитьРасчетПоКонтактнымЛицамПОКИ(Ссылка, МассивВидовКИ, Заполнено, Проверено) Экспорт
	
	Если ЗначениеЗаполнено(Ссылка) Тогда
		
		ВыборкаКЛ = Справочники.КонтактныеЛицаПартнеров.Выбрать(, Ссылка);
		Пока ВыборкаКЛ.Следующий() Цикл
			Для Каждого Эл Из МассивВидовКИ Цикл
				Если ВыборкаКЛ.КонтактнаяИнформация.Найти(Эл, "Вид") <> Неопределено Тогда
					Заполнено = Заполнено + 1;
				КонецЕсли; 
				Проверено = Проверено + 1;
			КонецЦикла;
		КонецЦикла;
		
	КонецЕсли;

	
	
КонецПроцедуры
