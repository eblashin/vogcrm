#Область ОбработчикиСобытий

&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполнения)
	// +CRM не переносить в объединенные решения
	//ВариантыОтчетовКлиент.ПоказатьПанельОтчетов("Администрирование", ПараметрыВыполнения);
	ВариантыОтчетовКлиент.ПоказатьПанельОтчетов("CRM_РазделАдминистрирование", ПараметрыВыполнения);
	// -CRM не переносить в объединенные решения
КонецПроцедуры

#КонецОбласти
